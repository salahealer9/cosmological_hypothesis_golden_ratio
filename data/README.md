# Data directory

- `raw/`: immutable downloaded chains and likelihood products. Large files are Git-ignored.
- `derived/`: reproducible transformations generated by scripts.

Commit manifests and checksums, not untracked manual edits. See `docs/DATA_PLAN.md`.
