# Notebooks

Notebooks are for transparent exploration and visual checks. Primary numerical results must also be reproducible through scripts and tests, so notebook execution order cannot become an undocumented dependency.
